/**
 * 输出格式化模块入口
 */

import MarkdownFormatter, { getMarkdownFormatter } from './markdown.js';
import { TerminalPatterns, detectPatterns, extractCodeBlocks, extractOptions } from './patterns.js';

export {
  MarkdownFormatter,
  getMarkdownFormatter,
  TerminalPatterns,
  detectPatterns,
  extractCodeBlocks,
  extractOptions
};

export default {
  MarkdownFormatter,
  getMarkdownFormatter,
  TerminalPatterns,
  detectPatterns,
  extractCodeBlocks,
  extractOptions
};
