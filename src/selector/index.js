/**
 * 选择检测模块入口
 */

import SelectionDetector, { getSelectionDetector } from './detector.js';
import SelectionHandler, { getSelectionHandler } from './handler.js';

export {
  SelectionDetector,
  getSelectionDetector,
  SelectionHandler,
  getSelectionHandler
};

export default {
  SelectionDetector,
  getSelectionDetector,
  SelectionHandler,
  getSelectionHandler
};
